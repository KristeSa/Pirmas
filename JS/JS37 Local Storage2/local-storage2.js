const fullName = document.body.querySelector("#name");
const splitFullName = fullName.split(" ")
const firstName = splitFullName[0]
const lastName = splitFullName[1]

const objFullName = JSON.parse(${firstName} ${lastName});
const arrFullName = arrFullName.push(objFullName)